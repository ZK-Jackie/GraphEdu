# GraphEdu PostgreSQL 自定义配置
# 覆盖默认配置中的关键项，其余使用 PostgreSQL 18 默认值

# 加载 Apache AGE 和登录延迟扩展
shared_preload_libraries = 'auth_delay,age'
auth_delay.milliseconds = '2000'

# 日志格式（支持 fail2ban）
log_line_prefix = '%t [%p] host=%h user=%u db=%d '
logging_collector = on

# 允许远程连接
listen_addresses = '*'
